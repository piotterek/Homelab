Code to decode the content of files downloaded from github. Use this in the node "code"

return items.map(item => {
    const base64String = $input.first().json.content;
    item.json.decoded = Buffer.from(base64String, 'base64').toString('utf8');
    return item;
});
